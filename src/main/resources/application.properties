# APIFootball Configuration
apifootball.api.url=https://apiv3.apifootball.com/
apifootball.api.key=9bb66184e0c8145384fd2cc0f7b914ada57b4e8fd2e4d6d586adcc27c257a978

# Server Port (optional, default is 8080)
# server.port=8080

# Spring Cache Configuration
spring.cache.type=caffeine
# Define cache names that will be used in the application.
# These caches will be created with default Caffeine settings unless configured further in Java config.
spring.cache.cache-names=leagues,standingsByLeague,teamStanding

# Example for more specific Caffeine configuration (can also be done in Java config)
# spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=3600s # Cache up to 500 entries, expire after 1 hour of inactivity

# Application specific properties
# Toggle for offline mode preference (true = try cache first, false = try live first)
# This is a conceptual property; its actual use will be implemented in the service logic.
app.features.prefer-cache-over-live=false
# Cache leagues data for 24 hours (86400 seconds)
app.features.cache-ttl-seconds.leagues=86400
# Cache standings data for 1 hour (3600 seconds)
app.features.cache-ttl-seconds.standings=3600

# Logging configuration (optional, Spring Boot provides sensible defaults)
# logging.level.com.example.footballapi=DEBUG # Set your package to DEBUG for more logs
# logging.level.org.springframework.web=INFO
# logging.level.org.springframework.cache=TRACE # To see cache interactions